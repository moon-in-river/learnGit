wow! fixed be bugfix/fix. changed by bugfix/change. There a error.
const { color:colorPalette } = this._options;
            const { smooth, symbol, symbolSize } = this._options;
            const series = map(dimensions.slice(1), (f, idx)=>({
                type: 'line',
                name: f.name,
                smooth,
                label: { show: showLabel },
                stack: enableStack ? 1 : 0,
                sampling: data.length > 2000 ? 'lttb' : undefined,
                symbol,
                symbolSize,
                areaStyle: {
                    opacity: 0.5,
                    color: new graphic.LinearGradient(0, 0, 0, 1, [{
                        offset: 0,
                        color: colorPalette[idx],
                    }, {
                        offset: 1,
                        color: 'rgba(255, 255, 255, 0)'
                    }])
                },
                data: map(data, (d, i)=>[categoryLabels[i], d[f.name]]),
            }));